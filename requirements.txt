#pip package names required by the package, and exact versions you know to work
click==7.1.2
networkx==2.6.3
matplotlib==3.5.1

#since I used conda I'm not sure those will work with pip
torch==1.11.0
torchvision==0.12.0
torchaudio==0.11.0

pandas==1.4.2
tqdm==4.64.0 # Not compulsory, but useful for checking progression of optimisation

